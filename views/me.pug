extends includes/layout

block content 
    h1=user
    h1=user.nickname
    if user.avatarUrl
        img(src=`/${user.avatarUrl}`, alt="", srcset="")
    else 
        img(src="/static/images/no_user.webp", alt="")
    a(href=`/user/update`) 유저 정보 수정 