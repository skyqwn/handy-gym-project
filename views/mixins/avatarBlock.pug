mixin avatarBlock(avatar, size)
    if avatar
        img(src=`/${avatar}` style={"width":size,"height":size}).avatar
    else
        img(src="/static/images/noUser.png" style={"width":size,"height":size}).avatar