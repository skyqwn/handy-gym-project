extends includes/layout

block content 
    each post in posts 
        a(href=`/post/${post.id}`) 
            h1=post.title
    div(id=totalPage, class="jsPageContainer")

    script(src="/static/js/postPagination.js") 